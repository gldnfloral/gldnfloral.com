# gldnfloral.com

Dead simple Astro (TypeScript) starter.

## Commands

- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm run lint`
- `npm run test`

Tests are powered by Vitest and live in `test/`.
